---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="页面未找到">
  <main class="min-h-screen flex flex-col items-center justify-center p-6 bg-gradient-to-b from-gray-50 to-white dark:from-gray-950 dark:to-gray-900 transition-colors duration-300">
    <div class="max-w-2xl text-center space-y-6 animate-fade-in-up">
      <div class="text-8xl font-bold bg-gradient-to-r from-purple-600 to-pink-600 dark:from-purple-400/90 dark:to-pink-400/90 bg-clip-text text-transparent">
        404
      </div>
      
      <h1 class="text-4xl font-bold text-gray-800 dark:text-gray-100/90 mb-4">
        页面消失在了数字宇宙中 🌌
      </h1>
      
      <p class="text-lg text-gray-600 dark:text-gray-300/80 mb-8">
        您寻找的页面可能已经迁移到其他维度，<br>
        或者暂时进入了量子隐形状态
      </p>

      <a href="/" class="inline-block px-8 py-3 text-lg font-medium text-white bg-purple-600 dark:bg-purple-700 rounded-lg hover:bg-purple-700 dark:hover:bg-purple-800 transition-colors duration-300 transform hover:-translate-y-1 shadow-lg hover:shadow-xl dark:shadow-purple-900/30">
        🚀 返回安全基地
      </a>
    </div>

    <!-- 星空背景适配暗色 -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
      {[...Array(20)].map((_, i) => (
        <div 
          class="absolute w-1 h-1 bg-purple-400/80 dark:bg-purple-200/80 rounded-full animate-star"
          style={`top: ${Math.random() * 100}%; left: ${Math.random() * 100}%; animation-delay: ${i * 0.2}s;`}
        />
      ))}
    </div>
  </main>

  <style is:global>
    @keyframes fade-in-up {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes star {
      0% { opacity: 0; transform: scale(1); }
      50% { opacity: 1; transform: scale(1.5); }
      100% { opacity: 0; transform: scale(1); }
    }
    .animate-fade-in-up {
      animation: fade-in-up 0.8s ease-out;
    }
    .animate-star {
      animation: star 2s infinite ease-in-out;
    }
  </style>
</BaseLayout>
